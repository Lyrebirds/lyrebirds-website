export class ContactMessage {
    constructor(private name: string, private email: string, private phone: string, private subject: string, private body: string) {
        // this.date = new Date();
    }
};